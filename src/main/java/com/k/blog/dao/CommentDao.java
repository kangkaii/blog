package com.k.blog.dao;import com.k.blog.mapper.mapper_old.CommentMapper_old;import com.k.blog.model.vo.CommentVo;import com.k.blog.model.vo.CommentVoExample;import org.apache.ibatis.annotations.Mapper;import org.apache.ibatis.annotations.Param;import org.apache.ibatis.annotations.Select;import org.apache.ibatis.annotations.SelectProvider;import org.apache.ibatis.jdbc.SQL;import java.util.List;/** * @author kangkai on 2018/7/9. */@Mapperpublic interface CommentDao {    //@Select("select * from t_comments limit #{pageNow} , #{limit}")    //List<CommentVo> getComments(@Param("pageNow") int pageNow, @Param("limit") int limit);    @SelectProvider(type = CommentDaoProvider.class, method = "getCommentList")    List<CommentVo> getCommentList();    @SelectProvider(type = CommentDaoProvider.class, method = "getCount")    Long getCount();    class CommentDaoProvider {        public String getCommentList() {            return new SQL(){{                SELECT("*");                FROM("t_comments");                ORDER_BY("created");            }}.toString();        }        public String getCount() {            return getCommentList().replace("*","count(*)");        }    }}